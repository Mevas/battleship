#ifndef BATTLESHIP_GLOBALS_H
#define BATTLESHIP_GLOBALS_H

namespace globals {
    const extern int boardNumCells;
    const extern int cellSize;
    const extern int borderWidth;
    const extern int boardSize;
    const extern int spaceBetweenBoards;

    const extern int windowWidth;
    const extern int windowHeight;
}

#endif //BATTLESHIP_GLOBALS_H
